#include <stdio.h>
#define input(buffer) fgets(buffer, sizeof(buffer), stdin); 

typedef struct board
{
	char position[3][3] = { {'0','0','0'},
							{'0','0','0'},
							{'0','0','0'}
						  };
}board_t;

int main() {
	
	return 0;
}
